{
  "name": "ondm",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:backend": "pnpm --filter @ondm/backend dev",
    "dev:frontend": "pnpm --filter @ondm/frontend dev",
    "build": "npm run build --workspace=frontend && node -e \"require('fs').cpSync('frontend/.next', '.next', {recursive:true}); require('fs').cpSync('frontend/public', 'public', {recursive:true})\"",
    "build:all": "pnpm --filter @ondm/backend build && pnpm --filter @ondm/frontend build",
    "build:backend": "pnpm --filter @ondm/backend build",
    "build:frontend": "pnpm --filter @ondm/frontend build",
    "start": "next start -p $PORT"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "next": "16.1.1"
  }
}
