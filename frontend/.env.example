NEXT_PUBLIC_API_URL=http://localhost:4000
# If empty or unset, the dashboard defaults to http://localhost:4000 (do not use "" unless you have an /api proxy on the Next host).
NEXT_PUBLIC_SITE_URL=https://ondm.com
NEXT_PUBLIC_GA_ID=
